# O app já aponta para o national.pmtiles no R2 público por padrão (ver App.jsx).
# Defina isto só para sobrescrever — ex.: servidor local ou domínio customizado
# (host com CORS + range requests).
# VITE_PMTILES_URL=http://localhost:8091/national.pmtiles
