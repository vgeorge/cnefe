# URL do arquivo PMTiles (host com CORS + range requests, ex.: Cloudflare R2 público).
# Copie para .env.local e ajuste. Sem esta variável, o app usa <origin>/national.pmtiles.
VITE_PMTILES_URL=https://<seu-bucket-r2>.r2.dev/national.pmtiles
